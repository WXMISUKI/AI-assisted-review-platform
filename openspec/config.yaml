schema: spec-driven

# Project context (optional)
# This is shown to AI when creating artifacts.
# Add your tech stack, conventions, style guides, domain knowledge, etc.
# Example:
#   context: |
#     Tech stack: TypeScript, React, Node.js
#     We use conventional commits
#     Domain: e-commerce platform
context: |
  Tech stack: TypeScript, React, Vite.
  Domain: enterprise AI-assisted construction plan review platform.
  Theme system: semantic tokens live in src/styles.css and must stay soft, readable, and consistent across light/dark themes.
  Theme changes should prefer root tokens and shared wrappers over one-off component overrides.
  Use the local skill .codex/skills/theme-system-guardrail/ for color audits and dark-mode cleanup.

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
