## 1. 规格收敛
- [x] 完成 proposal / design / specs 的最终自检，确认范围仅覆盖壳层、文档库入口、上传和详情承接。

## 2. 壳层与主题
- [x] 调整登录后平台壳层结构，让用户信息、角色和退出动作在所有 shell 页面都稳定可见。
- [x] 将侧栏视觉调整为浅色企业工作台风格，并补齐主题切换的 token 基础。

## 3. 文档库入口
- [x] 将文档库上传区域改成支持拖拽和点击选择文件。
- [x] 为上传补充可选的文件名和项目名称输入，并使用默认值兜底。
- [x] 移除文档库主路径中的快速预览入口。

## 4. 详情页承接
- [x] 将文档详情从右侧嵌套承载改为全屏工作页，并补充返回按钮。
- [x] 保持现有 mock 审查工作台复用，但调整接入方式适配新入口。

## 5. 验证与归档
- [x] 运行最小化验证并检查关键页面状态。
- [x] 完成后归档本 change。
